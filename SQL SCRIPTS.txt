create table Smoothie (
	id INT,
	PRIMARY KEY(id),
	name VARCHAR NOT NULL,
	description VARCHAR NOT NULL,
	kcal VARCHAR NOT NULL,
	fat VARCHAR NOT NULL,
	saturates VARCHAR NOT NULL,
	carbs VARCHAR NOT NULL,
	sugars VARCHAR NOT NULL,
	fibre VARCHAR NOT NULL,
	protein VARCHAR NOT NULL,
	salt VARCHAR NOT NULL
)

INSERT INTO Smoothie(id,name, description,kcal,fat,saturates,carbs,sugars,fibre,protein,salt)
VALUES (1, 'Two-minute breakfast smoothie', 'Whizz up a low fat breakfast smoothie in no time. Use banana with other soft fruit, plus honey for a little sweetness and oats for slow-release fuel', '156','3g','2g','25g','19g','2g','4g','0.1g');
INSERT INTO Smoothie(id,name, description,kcal,fat,saturates,carbs,sugars,fibre,protein,salt)
VALUES (2, 'Peanut butter smoothie', 'Whizz up a quick and filling peanut butter smoothie, with rolled oats and banana. Brilliant for breakfast, you can freeze the banana for extra thickness', '193','7g','2g','26g','16g','3g','4g','0.18g');
INSERT INTO Smoothie(id,name, description,kcal,fat,saturates,carbs,sugars,fibre,protein,salt)
VALUES (3, 'Kale smoothie', 'Give yourself a dose of vitamin C in the morning with this vegan green smoothie. Along with kale and avocado, there''s a hit of zesty lime and pineapple', '152','11g','2g','8g','3g','2g','4g','0.1g');
INSERT INTO Smoothie(id,name, description,kcal,fat,saturates,carbs,sugars,fibre,protein,salt)
VALUES (4, 'Breakfast smoothie', 'Make the most of the berry season with a glass of fruity goodness', '124','1g','0.1g','25g','24g','5g','2g','0.1g');
INSERT INTO Smoothie(id,name, description,kcal,fat,saturates,carbs,sugars,fibre,protein,salt)
VALUES (5, 'Strawberry smoothie', 'Get your fruit fix with our strawberry smoothie made with banana and orange juice. It''s free from dairy, so it''s vegan too – making it a great start to anyone''s day', '170','1g','0.1g','34g','32g','8g','3g','0.01g');


CREATE TABLE ROLE (
	ID INT PRIMARY KEY,
	NAME VARCHAR
);

CREATE TABLE USERS (
	ID INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
	USERNAME VARCHAR,
	EMAIL VARCHAR,
	PASSWORD VARCHAR
)

CREATE TABLE USER_ROLES(
	USER_ID INT REFERENCES USERS(ID),
	ROLE_ID INT REFERENCES ROLE(ID),
	CONSTRAINT USER_ROLES_PK PRIMARY KEY (USER_ID, ROLE_ID)
)
INSERT INTO ROLE (ID, NAME) VALUES (1, 'BUSINESS_OWNER');
INSERT INTO ROLE (ID, NAME) VALUES (2, 'USER');
